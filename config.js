// Cambiá esta URL por la de tu backend cuando lo despliegues (Render/Railway).
// En local, si corrés el backend con "npm run dev", queda en el puerto 5000.
const API_URL = 'http://localhost:5000/api';
